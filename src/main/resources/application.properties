spring.jpa.hibernate.ddl-auto=update 
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/ApiResDatabaseV2?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=12345
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect


#spring.application.name=ApiRest
# Configuración de Spring Data JPA

# Esto actualiza el esquema de la base de datos automáticamente.

# Configuración de la conexión a la base de datos
#spring.datasource.url=jdbc:mysql://localhost:3306/testdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# URL de conexión a la base de datos MySQL.

#spring.datasource.username=root 
# Nombre de usuario para la conexión.

#spring.datasource.password=12345
# Contraseña para la conexión.

# Configuración del dialecto de Hibernate
#spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# Dialecto de Hibernate para MySQL 8.